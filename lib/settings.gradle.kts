rootProject.name = "skueeze"

pluginManagement {
    dependencyResolutionManagement {
        repositories {
            gradlePluginPortal()
        }
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}